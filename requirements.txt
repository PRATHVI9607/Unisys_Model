# KubeHeal v4 — top-level requirements
# Core ML
torch==2.2.0
torch-geometric>=2.5.0
numpy<2
scikit-learn>=1.3.0

# Explainability + LLM
shap==0.44.0
anthropic

# Serving
fastapi==0.110.0
uvicorn[standard]==0.27.0
pydantic==2.6.0

# Agents
redis>=5.0.0
kubernetes-asyncio==29.0.1
aiohttp==3.9.0
PyYAML>=6.0.0

# Export
onnx==1.16.0
onnxruntime==1.17.0
onnxconverter-common>=1.14.0
minio>=7.2.0  # optional: model registry upload

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# NOTE: mamba-ssm and causal-conv1d intentionally removed in v4 (BiLSTM replaces Mamba)
# NOTE: Kafka removed in v4 (Redis Sentinel provides HA)
